#Input function and boolean comparisons
#Input default intake is string 
print("Please enter the same number twice:")
str_num=input()
num= float(input())
#Since the comparison operator can compare between two different datatypes 
print("Comparison between the str_num==num:",str_num==num,"Since even though the numbers entered are same the datatypes they are stored in are completely different")
